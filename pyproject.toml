[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aat-core"
version = "0.1.0"
description = "Shared core for AAT Studio Tools"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["core"]
